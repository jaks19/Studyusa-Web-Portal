<% include ./partials/headerUser %>
<%if (user.admin){ %>
  <div class='row'>
    <div class='col-xs-12 col-md-4 col-md-offset-0'>        
        <nav aria-label="..." style='margin-top:-10%; margin-bottom:3%;'>
          <ul class="pager">
            <li class="previous"><a href="/index/<%=user.username%>"><span aria-hidden="true">&larr;</span> Administrator Control Panel </a></li>
          </ul>
        </nav>
    </div>
  </div>
<% } %>
    
    
    
    
    
    <div class='row'>
    
    <div class='col-xs-12 col-md-3'>
        <% include ./partials/profileCorner %>
    </div>
    
    <div class='col-xs-12 col-md-6'>
        <% include ./partials/fileSubmissionHistory %>
    </div>

    <div class='hidden-sm hidden-xs col-md-3'>
        <% include ./partials/newsRoll %>
    </div>
    
    <% if (firstContact){ %>
      <div class="modal fade bs-example-modal-lg" id="greet" role="dialog" aria-labelledby="myModalLabel">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-body">
                  <% include ./partials/notifsWelcome %>
                </div>
            </div>
        </div>
    </div>            
  <% } %>  
</div>
<script>
    var spansList = $('.comment > .timestring');
  
  $.each(spansList, function( i, val ) {
        var timeDateText = $(val).text();
        var momentObj = moment(timeDateText);
        var timespent = momentObj.fromNow();
        $(this).text(timespent);
  });
    $(document).ready(function(){
        $('#greet').modal('show');
    });
</script>