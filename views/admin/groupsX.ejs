<% include ../partials/headerUser %>

<div class='new' align='center'>
    <input class="search" id="myInput" placeholder="Search for a Group" autofocus>
    <button class="sort-choosable"><a class='blend' href ="/index/<%=user.username%>">By name</a></button>
    <button class="sort-chosen">By Group</button>
</div>

<div class='row' style='padding: 2%;'>
    <div class='col-xs-12' align='center'>
        <div class="container bootstrap snippet">
            <div class="row">
                <div class="col-lg-12">
                    <div class="main-box no-header clearfix">
                        <div class="main-box-body clearfix">
                            <div class="table-responsive">

                                <% groups.forEach(function(group){ %>
                                    <% include ../modals/modalManageUsersGroup %>
                                <% });%>
                                <!-- Insert any table of this format -->
                                <table id="myTable" class="table group-list">
                                    <thead >
                                        <tr>
                                        <th style='padding-left: 20px;'><span>Groups</span></th>
                                        <th><span>Last Meeting</span></th>
                                        <th><span>Next Meeting</span></th>
                                        <th class="text-center"><span>Status (etc)</span></th>
                                        <th><span>Email</span></th>
                                        <th>&nbsp;</th>
                                        </tr>
                                    </thead>

                                    <tbody>
                                        <div class='row'>
                                            <div class='col-xs-12'>

                                                <% d = new Date(Date.now()) %>
                                                <% groups.forEach(function(group){ %>
                                                    <tr id='<%=group.name%>' class='groups-table'>
                                                        <td id='<%=group.name%>' class='main-td' style='padding-left: 20px;'>
                                                            <span class="group-subhead"><%= group.name %></span>
                                                            <a href="/index/<%=user.username%>/groups/<%=group._id%>" id='<%=group.name%>' class="group-link">Expand (<%= group.users.length %>) member records</a>
                                                        </td>
                                                        <td id='<%=group.name%>' class='main-td'>
                                                            <div class='comment'>
                                                            <span class='timestring text-muted '><%= d.toISOString() %></span>
                                                            </div>
                                                        </td>
                                                        <td id='<%=group.name%>' class='main-td'>
                                                            <div class='comment'>
                                                            <span class='timestring text-muted '><%= d.toISOString() %></span>
                                                            </div>
                                                        </td>
                                                        <td id='<%=group.name%>' class="text-center" class='main-td'>
                                                            <span class="label label-default">pending</span>
                                                            <!--  TODO:Can use label-success for green, label-danger etc etc -->
                                                            <!--  Perhaps cool to use with the deadline/pace tracking system -->
                                                        </td>
                                                        <td id='<%=group.name%>' class='main-td'>
                                                            <a href="#">marlon@brando.com</a>
                                                        </td>
                                                        <td id='<%=group.name%>' style="width: 20%;" class='main-td'>

                                                            <a href='/index/<%=user.username%>/groups/<%=group._id%>/messages' class=' btn btn-sm btn-primary glyphicon glyphicon-comment'></span></a>
                                                            <a href="#add-<%=group._id%>" data-toggle="modal" data-target='#add-<%=group._id%>' class=' btn btn-sm btn-success glyphicon glyphicon-plus'></span></a>
                                                            <% include ../modals/modalAddUsersToGroup %>
                                                            <a href="#users-<%=group._id%>" data-toggle="modal" data-target='#users-<%=group._id%>' class=' btn btn-sm btn-success glyphicon glyphicon-user'></span></a>

                                                            <a href='#delete-<%=group._id%>' data-toggle="modal" data-target="#delete-<%=group._id%>" class=' btn btn-sm btn-danger glyphicon glyphicon-trash'></span></a>
                                                            <% include ../modals/modalDeleteGroup %>
                                                        </td>

                                                    </tr>

                                                <% });%>
                                            </div>
                                        </div>

                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>


<script>
// Contexts for notifications etc at login
    $(document).ready(function(){
        var context = $('#whistlerContext').val();
        if (context == 'justLoggedIn') { $('#greet').modal('show') }
        else if (context == 'invitation') { $('#invitationModal').modal('show') }

        // Trigger search on loading of page
        // $('#myInput').attr('value','groupA');
        // var e = jQuery.Event("keyup");
        // $("#myInput").trigger(e);
    });
</script>

<script>
// Some primitive css
      $('#dashboard').css("background-color", "#FFFFFF");
      $('#dashboardtxt').css("color", "#0084b4");
</script>

<script>

// For search bar

// (Each one needs a slightly different script due to how the table is formatted)
// But like in this document, tables of same format can just be plugged in and are good to go
// Assuming all tables use same field rank to search on
$('#myInput').keyup(function () {
  var input, filter, table, tr, td, i, keep;
  input = $("#myInput")[0];
  filter = input.value.toUpperCase();
  table = $("#myTable");
  tbody = table[0].getElementsByTagName("tbody");
  trs = tbody[0].getElementsByTagName("tr");


  for (i = 0; i < trs.length; i++) {
    tds = trs[i].getElementsByTagName("td");
    if (tds.length > 0){
        // Only pick the tds with values to be compared to search term
        if (tds[0].className === "main-td"){
            if (tds[0].id.toUpperCase().indexOf(filter) > -1) {
                // The whole tr with that td is deleted if not relevant
                trs[i].style.display = "";
            }
            else {
                trs[i].style.display = "none";

            }
        }
    }
  }
});
</script>


<style>
/* Search bar and buttons CSS */
.new input {
    border-radius: 25px;
    padding: 0.8% 1.2%;
    background-color: white;
    border: solid 1px rgba(0, 0, 0, 0.2);
    width: 35%;
    box-sizing: border-box;
    color: #3e9ddc;
    margin-bottom: 5px;
}

input:focus, textarea, button:focus {
    outline: none !important;
}
.new button {
    font-size: 100%;
    padding: 5px 15px;
    border-radius: 25px;
    border: none;
    text-decoration: none;
    margin: 2px 0;
}

.sort-chosen{
    color: #eeeeee;
    background-color: #afc2d4;
    cursor: default;
}

.sort-choosable {
    color: #eeeeee;
    background-color: rgb(46, 111, 168);
}

a.blend:link,
a.blend:visited,
a.blend:hover,
a.blend:active
{
   color: white;
   text-decoration: none;
}

::-webkit-input-placeholder { /* Chrome/Opera/Safari */
  color: #777777b8;
}
::-moz-placeholder { /* Firefox 19+ */
  color: #777777b8;
}
:-ms-input-placeholder { /* IE 10+ */
  color: #777777b8;
}
:-moz-placeholder { /* Firefox 18- */
  color: #777777b8;
}

</style>


<style>
/*  The Table CSS */
table#myTable > tbody > tr > td {
    vertical-align: middle;
}

body{
    background:#eee;
}
.main-box.no-header {
    padding-top: 20px;
}
.main-box {
    background: #eeeeee;
    height: 80%;
    overflow:auto;
}
.table a.table-link.danger {
    color: #e74c3c;
}
.label {
    border-radius: 3px;
    font-size: 0.875em;
    font-weight: 600;
}

.user-list tbody td .user-subhead {
    font-size: 0.875em;
    font-style: italic;
}

.group-list tbody td .group-subhead {
    font-size: 29px;
}

.user-list tbody td .user-link {
    display: block;
    font-size: 1.25em;
    padding-top: 3px;
    margin-left: 60px;
}

.group-list tbody td .group-link {
    display: block;
    font-size: 17px;
    padding-top: 3px;
    font-weight: 340;
}

a {
    color: #3498db;
    outline: none!important;
}

tr.users-table {
    display: none;
}

tr.users-table > td > img {
    position: relative;
    max-width: 50px;
    max-height: 50px;
    float: left;
    margin-right: 15px;
}

tr.groups-table > th {
    text-transform: uppercase;
    font-size: 0.875em;
    border-bottom: 2px solid #e7ebee;
}

tr.users-table > th {
    text-transform: uppercase;
    font-size: 0.875em;
    border-bottom: 2px solid #e7ebee;
}

/* Widths of columns */

tr.groups-table > td:first-child {
    font-size: 1.125em;
    font-weight: 300;
    width: 35%;
}

tr.groups-table > td:nth-child(2) {
    width: 15%;
}

tr.groups-table > td:nth-child(3) {
    width: 15%;
}

tr.groups-table > td {
    font-size: 0.875em;
    padding: 12px 8px;
    background-color: #ffffff;
}


/*  People */

tr.users-table > td:nth-child(1) {
    font-size: 1.125em;
    font-weight: 300;
    width: 35%;
}

/*  Last Online */
tr.users-table > td:nth-child(2) {
    width: 15%;
}

tr.users-table > td {
    font-size: 0.875em;
    padding: 12px 8px;
}
 </style>

<% include ../../front-end/timestampToElapsed %>
