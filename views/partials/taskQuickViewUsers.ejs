<!-- Already should prevent any admin from being a taskSubscriber so we enter here with this assumption -->
<% if (task.taskSubscribers.length != 0){ %>

    <!-- Make the processing be in js in future -->
    <div id="container" style='width:auto; margin-left:3%; margin-top:-10%;'>

        <div id="arrowL">
            <span class='glyphicon glyphicon-menu-left' style='margin-top:300%; color:#949494;'></span>
        </div>

        <div id="list-container" style='width:94%; background-color:red;'>
            <div class='lister' style='float:left;'>

                <!-- Your repeats go here see commented-out example -->
                <!--  Use js to enter the divs inside the div class='list' using this example-->
                <!-- <div class='item'>
                </div> -->
                <% task.taskSubscribers.forEach(function(taskSubscriber){ %>

                    <% if (!taskSubscriber.user.admin) { %>

                            <div class='item' style='height:auto;'>
                                <!-- Make both the image and the name be the link -->
                                <a class='names' href="/index/<%=user.username%>/tasks/<%=task._id%>/dashboard/<%=taskSubscriber.user._id%>" class="user-link" style="font-size:12px;">

                                    <div id='hovy' style='height:50px;'>
                                        <img src="https://bootdey.com/img/Content/user_1.jpg" alt="">
                                    </div>

                                    <div id='bott' style='height:10px;'>
                                        <%=taskSubscriber.user.name.slice(0,1).toUpperCase() + taskSubscriber.user.name.slice(1,30)%>

                                    </div>
                                </a>

                            </div>
                    <% } %>

                <% });%>
            </div>
        </div>

        <div class="arrow-right" id="arrowR">
            <span class='glyphicon glyphicon-menu-right' style='margin-top:300%; color:#949494;'></span>
        </div>

    </div>

<% } %>




<style>

#list-container {
overflow:hidden;
float:left;
}

.lister{
    background:#e6e4e4;
    min-width:1400px;
    float:left;
    height:90px;
}

#arrowR{
    width:3%;
    height:90px;
    float:right;
    cursor:pointer;
    z-index:10;
}

#arrowL{
    width:3%;
    height:90px;
    float:left;
    cursor:pointer;
    z-index:10;
}

.item {
    /* Fix width of the card */
    width:5%;
    height:100%;
    margin:5px;
    float:left;
    position:relative;

    opacity: 0.7;
    filter: alpha(opacity=70); /* For IE8 and earlier */
    position: relative;
    z-index:10;
}

.item:hover {
    opacity: 0.85;
    filter: alpha(opacity=85); /* For IE8 and earlier */
}

a.names:link,
a.names:visited,
a.names:hover,
a.names:active
{
   color: #5aa4e0;
   text-decoration: none;
}

</style>


<script src=/scripts/views/partials/taskQuickViewUsers.js></script>


<style>

    a {
        color: #3498db;
        outline: none !important;
    }

    img {
        max-width: 50px;
        max-height: 50px;
    }

</style>
